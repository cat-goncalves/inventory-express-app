<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Inventory</title>
  <link rel="shortcut icon" href="favicon.ico">
  <link rel="stylesheet" href="font-awesome.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif:wght@400;700&family=Roboto:wght@400;700&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>

<body>
  <div class="container">
    <h1>Inventory</h1>
    <p>Add an item along with its price to add it to your inventory list. Once you have added an item, update the
      quantity
      to calculate the total cost.</p>
    <div class="row">
      <div id="merchandise" class="col-sm 6">
        <h3>Merchandise</h3>
        <ul class="messages">
          <% for(var i=0; i<items.length; i++) {%>
            <li class="message">
              <span><span class="label">Item: </span><%= items[i].item %></span>
              <form id ="units">
                <input type="number" step="0.1" min="0" placeholder="units" name="units">
                <button class="unitSubmit" type="button">Update</button>
              </form>
              <span class="block"><span class="label">Price: </span> &#36;<%= items[i].price %></span>
              <% let total=items[i].price * items[i].quantity %>
                <span class="block"><span class="label">Quantity: </span><%= items[i].quantity %> units</span>
                <span><span class="label">Total Cost: </span>: &#36;<%= total %></span>
                <span><i class="fa fa-trash" aria-hidden="true"></i></span>
            </li>
            <% } %>
        </ul>
      </div>
      <div class="col-sm-6">
        <h4>Add products to inventory list:</h4>

        <form action="/inventory" method="POST">
          <input type="text" placeholder="name" name="item">
          <input type="number" step="0.01" min="0" placeholder="price" name="price">
          <button type="submit">Submit</button>
      </div>
      </form>
    </div>
  </div>
  <script src="main.js"></script>

</body>

</html>