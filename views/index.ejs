<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Inventory App</title>
  <link rel="shortcut icon" href="favicon.ico">
  <link rel="stylesheet" href="font-awesome.min.css">
  <link rel="stylesheet" href="styles.css">
</head>

<body>
  <h1>Inventory</h1>
  <h2>Add an item along with its price to update the inventory:</h2>


  <h3>Inventory</h3>
  <ul class="messages">
    <% for(var i=0; i<items.length; i++) {%>
      <li class="message">
        <span>
          <%= items[i].item %>
        </span>
        <span>&#36;<%= items[i].price %></span>
        <form action="/update-units" method="post">
          <input type="number" step="0.1" min="0" placeholder="units" name="units">
          <button class="unitSubmit" type="submit">update</button>
        </form>
        <span>
          Shelf Cost: <%= items[i].price * units[i].unit %>
        </span>
        <span><i class="fa fa-trash" aria-hidden="true"></i></span>
      </li>
      <% } %>
  </ul>

  <h2>Input a Product to Inventory</h2>

  <form action="/inventory" method="POST">
    <input type="text" placeholder="name" name="item">
    <input type="number" step="0.01" min="0" placeholder="price" name="price">
    <!-- <input type="number" step="0.1" placeholder="units" name="units"> -->

    <button type="submit">Submit</button>
  </form>

  <script src="main.js"></script>

</body>

</html>